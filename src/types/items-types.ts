// Item type to handle items in the inventory
export type Item = {
  id: string;
  name: string;
  price: number;
  quantity: number;
  description: string;
  createdAt: string;
  updatedAt: string;
};
